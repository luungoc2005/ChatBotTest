import styled from 'styled-components';
import {PanelFooter} from 'rebass';

export default styled(PanelFooter)`
  position: relative;
  border-top: 2px solid rgba(0,0,0,0.2);
  padding: 16px 24px;
`;